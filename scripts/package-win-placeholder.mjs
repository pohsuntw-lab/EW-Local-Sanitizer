console.error("Windows packaging is not implemented in the plain-text vertical slice.");
console.error("Public release remains blocked until the Electron UI, packaging and Authenticode signing are complete.");
process.exit(1);

